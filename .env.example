# Backend orchestrator env example
# Copy to .env (DO NOT commit .env)
#   cp .env.example .env

# --- PostgreSQL (shared by both services) ---
POSTGRES_DB=uav
POSTGRES_USER=uav_user
POSTGRES_PASSWORD=change_me
# Host port for local access (DB is still bound to 127.0.0.1 in compose)
POSTGRES_PORT=5433

# --- Public service ports (host -> container) ---
TELEMETRY_PORT=8000
MEDIA_PORT=8001

# --- Optional ZLMediaKit settings (currently not used by backend flows) ---
# Keep ZLM_HOST non-empty so generated playback URLs are sane.
# Leave ZLM_SECRET empty unless you plan to integrate ZLM HTTP API.
ZLM_HOST=http://localhost:9000
ZLM_SECRET=

# --- Misc ---
TZ=UTC
LOG_LEVEL=INFO
